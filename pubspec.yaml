name: mispedidos
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.1

  # Bases de datos
  get_storage: ^2.0.3
  hive: ^2.2.1
  hive_flutter: ^1.1.0
  objectbox: ^1.6.0
  #---------------

  path_provider: ^2.0.11
  provider: ^6.0.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  hive_generator: ^1.1.3
  build_runner: ^2.2.0

flutter:
  uses-material-design: true
  assets:
    - lib/resources/productos/
    - lib/resources/sabores/